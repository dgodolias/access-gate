# access-gate

Plug-and-play shared-password gate for web apps. One env var locks the entire
site (pages, API, static assets) behind a login page; unset it and the library
does nothing.

Full documentation lands with the first release. See `.env.example` for the
variables and `CHANGELOG.md` for what is in.
