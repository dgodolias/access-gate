# Copy to .env.local (or set in the Vercel dashboard). Never commit the value.
ACCESS_GATE_PASSWORD=
# Optional: paths that stay public
ACCESS_GATE_EXEMPT=/api/health,/api/cron/*
# The local runner (serve.mjs) is not on Vercel, so tell the gate to run anyway:
ACCESS_GATE_ALWAYS=1
